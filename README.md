# City Service Finder Portal (React + Tailwind + Vite)

A clean, professional frontend starter for a City Service Finder Portal.

## ✨ Features
- Home with hero + search
- Search page with filters and results grid
- Service detail page
- Login & Signup pages
- Reusable components (Navbar, SearchBar, ServiceCard, RatingStars, Footer)
- Tailwind utility classes + small component presets

## 🚀 Run locally
```bash
# 1) Extract the zip
# 2) Install deps
npm install
# 3) Start dev server
npm run dev
```

The app runs on http://localhost:5173 by default.
